import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#ff4d30"
        },
        secondary: {
            main: "#130f40"
        }
    },
    typography: {
        fontFamily: "Poppins"
    }
})

export default theme